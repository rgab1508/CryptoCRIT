var firebase = require("firebase-admin");

class Student {
  constructor(rollno) {
    this.rollno = rollno;
  }

  async getEmail() {
    // TODO write logic to fetch student email from database
  }
}
